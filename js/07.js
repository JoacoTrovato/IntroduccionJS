// Orden de las operaciones

let resultado;

resultado = (20 + 30) * 2;

resultado = (600 + 600) * 1.05; // Para sacar porcentaje

// console.log(resultado);

// Incrementos 

let puntaje = 10;

// Incremento en 1
puntaje++; // 10
++puntaje; // 12

// Decremento en 1
puntaje--;
--puntaje;

// Incremento en valor deseado
puntaje += 10; // Incrementa el puntaje en el valor que asignemos después del +=

console.log(puntaje);